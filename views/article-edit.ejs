<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            margin:0;
            padding:0;
        }

        #wrapper {
            width:100%;
            height:100vh;
            display:flex;
            flex-direction:column;
        }

        #wrapper > form {
            width:30%;
            display:flex;
            flex-direction:column;
        }

        #wrapper > form > input, textarea {
            padding:8px;
            margin:8px;
            font-size:17px;
            outline:none;
        }

        #wrapper > form > label {
            margin:8px;
            font-size:17px;
        }

        #wrapper > form > textarea {
            height:150px;
        }

        #wrapper > form > div > button {
            font-size:15px;
            padding:8px 12px;
            margin:8px;
        }

        #wrapper > form > div > a {
            text-decoration:none;
            color:blue;
            font-size:15px;
            padding:8px;
            margin:8px;
            font-size:17px;
        }

        #wrapper > h1 {
            border-bottom:1px solid black;
            padding-bottom:6px;
            margin-bottom:16px;
            margin-left:10px;
            margin-right:10px;
            text-align:center;
        }
    </style>
    <title>Document</title>
</head>
<body>
    <div id="wrapper">
        <form method="post" action="/article/edit/<%= article._id %>?_method=PATCH">
            <label for="title">Title</label>
            <input type="text" id="title" value="<%= article.title %>" name="title"/>
            <label for="description">Description</label>
            <input type="text" id="description" value="<%= article.description %>" name="description"/>
            <label for="markdown">Markdown</label>
            <textarea id="markdown" name="markdown"><%= article.markdown %></textarea>
            <div>
                <a href="/">cancel</a>
                <button type="submit">edit</button>
            </div>
        </form>
    </div>
</body>
</html>