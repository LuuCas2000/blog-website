<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        * {
            padding:0;
            margin:0;
        }

        #wrapper {
            display:flex;
            flex-direction:column;
        }

        nav {
            width:100%;
            display:flex;
            align-items:center;
            justify-content:center;
        }

        nav > #nav__btns {
            width:50%;
            margin:30px;
            display:flex;
            align-items:center;
            justify-content:center;
        }

        nav > #nav__btns > ul li {
            list-style-type:none;
            display:inline;
            margin:10px 12px;
            padding:4px 0;
        }

        nav > #nav__btns > ul li > a {
            text-decoration:none;
            color:black;
        }

        nav > #nav__btns > ul li > a:hover {
            padding-bottom:4px;
            border-bottom:1px solid black;
        }

        #wrapper > #intro {
            width:90%;
            align-self:center;
            margin:30px 0;
        }

        #wrapper > #search_bar {
            width:90%;
            align-self:center;
            display:flex;
            align-items:center;
            justify-content:center;
            margin-bottom:50px;
        }

        #wrapper > #search_bar > #input_div {
            width:40%;
        }

        #wrapper > #search_bar > #input_div > input {
            width:100%;
            padding:6px 4px;
            outline:none;
            font-size:17px;
            text-indent:8px;
        }

        #wrapper > #intro > h1 {
            font-size:90px;
            font-weight: lighter;
            text-align:center;
        }

        #wrapper > #intro > h3 {
            font-size:30px;
            font-weight: lighter;
            text-align:center;
            margin:20px 0;
        }

        #wrapper > #articles {
            width:90%;
            display:flex;
            flex-wrap:wrap;
            align-self:center;
            align-items:center;
            justify-content:center;
            margin-bottom:30px;
        }

        #wrapper > #articles > #article {
            position:relative;
            width:420px;
            height:auto;
            min-height:450px;
            display:flex;
            flex-direction:column;
            margin:16px;
        }

        #wrapper > #articles > #article > #article__img {
            width:100%;
            height:200px;
        }

        #wrapper > #articles > #article > #article__img > img {
            width:100%;
            height:200px;
        }

        #wrapper > #articles > #article > #article__title {
            width:100%;
            padding:10px 0;
        }

        #wrapper > #articles > #article > #article__title > span {
            font-size:25px;
            text-align:justify;
            display:block;
            font-weight:600;
        }

        #wrapper > #articles > #article > #article_date {
            width:100%;
            padding-bottom:10px;
        }

        #wrapper > #articles > #article > #article_date > span {
            font-size:17px;
            display:block;
        }

        #wrapper > #articles > #article > #article__desc {
            width:100%;
        }

        #wrapper > #articles > #article > #article__desc > span {
            font-size:17px;
            text-align:justify;
            display:block;
            font-weight:lighter;
        }

        #wrapper > #articles > #article > #btns {
            position:absolute;
            width:100%;
            padding-top:16px;
            bottom:0;
        }

        #wrapper > #articles > #article > #btns > a {
            text-decoration:none;
            font-size:17px;
            color:blue;
            padding-bottom:4px;
            border-bottom:1px solid blue;
        }

    </style>
    <title>Lucas Codes - Blog</title>
</head>
<body>
    <div id="wrapper">
        <nav>
            <div id="nav__btns">
                <ul>
                    <li><a href="#">HOME</a></li>
                    <li><a href="#">CONTACT</a></li>
                    <li><a href="#">ABOUT</a></li>
                </ul>
            </div>
        </nav>

        <div id="intro">
            <h1>Lucas <span style="font-weight: 900; padding-bottom: 1px; border-bottom: 5px solid black;">Codes</span></h1>
            <h3>Creating content about web development and technology</h3>
        </div>

        <div id="search_bar">
            <div id="input_div"><input type="search" placeholder="search articles" name="searchbar"/></div>
        </div>

        <div id="articles">
            <% articles.forEach(article => { %>
                <% if (article.length === 0) { %>
                    <span>no articles yet</span>
                <% } %>
                <div id="article">
                    <div id="article__img">
                        <img src="./images/<%= article.image %>" alt="img" />
                    </div>
                    <div id="article__title">
                        <span><%= article.title %></span>
                    </div>
                    <div id="article_date">
                        <span>02/03/2000</span>
                    </div>
                    <div id="article__desc">
                        <span><%= article.description %></span>
                    </div>
                    <div id="btns">
                        <a href="/articles/<%= article.slug %>">Read more</a>
                    </div>
                </div>
            <% }) %>
        </div>
        
    </div>
</body>
</html>