<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        *  {
            margin:0;
            padding:0;
        }

        #form__div {
            width:100%;
            height:100vh;
            display:flex;
            align-items:center;
            justify-content:center;
            flex-direction:column;
        }

        #form__div > form {
            width:30%;
            display:flex;
            flex-direction:column;
        }

        #form__div > form > input {
            padding:8px;
            margin:8px;
            font-size:17px;
            outline:none;
        }

        #form__div > form > button {
            width:150px;
            font-size:15px;
            padding:8px;
            margin:8px;
            align-self:center;
        }

        #form__div > h1 {
            border-bottom:1px solid black;
            padding-bottom:6px;
            margin-bottom:16px;
            margin-left:10px;
            margin-right:10px;
            text-align:center;
        }

        #form__div > span {
            margin-top:16px;
            font-size:17px;
            margin-left:10px;
            margin-right:10px;
            text-align:center;
        }

        #form__div > span > a {
            text-decoration:none;
            color:blue;
        }

        @media screen and (max-width: 1130px) {
            #form__div > form {
            width:50%;
            display:flex;
            flex-direction:column;
        }
        }

        @media screen and (max-width: 600px) {
            #form__div > form {
            width:80%;
            display:flex;
            flex-direction:column;
        }
        }
    </style>
    <title>User login</title>
</head>
<body>
    <div id="form__div">
        <h1>Log in</h1>
        <form method="post" action="/user/login">
            <input type="text" placeholder="username" id="username" name="username"/>
            <input type="text" placeholder="password" id="password" name="password"/>
            <button type="submit">log in</button>
        </form>
        <span>Don't have an account yet ? <a href="/user/create">Create new account</a></span>
    </div>
</body>
</html>